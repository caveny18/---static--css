/* ===========================
   🌐 JJ TECNOLOGÍA - Modern Fintech Style
   =========================== */

:root {
  --color-primary: #077ca0;
  --color-secondary: #049e4c;
  --color-accent: #00b4d8;
  --color-dark: #001c3d;
  --text-light: #f8fbff;
  --text-muted: #bcdfff;
}

/* ---- GLOBAL ---- */
body {
  font-family: "Poppins", sans-serif;
  background: linear-gradient(180deg, #0a0a0f, #00182a);
  color: var(--text-light);
  overflow-x: hidden;
}

a {
  text-decoration: none;
  color: inherit;
}

section {
  scroll-margin-top: 80px;
}

/* ---- NAVBAR ---- */
.navbar-custom {
  background: rgba(0, 20, 40, 0.6);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
  transition: background 0.4s ease;
}

.navbar-custom.scrolled {
  background: rgba(0, 20, 40, 0.95);
}

.navbar-brand img {
  width: 130px;
  transition: transform 0.3s;
}
.navbar-brand img:hover {
  transform: scale(1.05);
}

.navbar-nav .nav-link {
  color: var(--text-muted) !important;
  font-weight: 500;
  transition: color 0.3s;
}
.navbar-nav .nav-link:hover {
  color: var(--color-accent) !important;
}

/* ---- HERO ---- */
.hero-section {
  height: 90vh;
  background: url('../img/imag1.jpg') center/cover no-repeat;
  position: relative;
}
.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(180deg, rgba(0,0,0,0.7), rgba(0,30,60,0.9));
  z-index: 1;
}
.hero-section .container {
  z-index: 2;
  position: relative;
}
.hero-section h1 {
  font-size: 3rem;
  text-shadow: 0 0 25px rgba(0, 191, 255, 0.5);
}
.hero-section p {
  opacity: 0.9;
}

/* ---- BENEFICIOS ---- */
.beneficios {
  background: #f8fbff;
  color: #001c3d;
}
.beneficios h2 {
  background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
}
.card-beneficio {
  background: rgba(255, 255, 255, 0.75);
  border: none;
  border-radius: 20px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  transition: transform 0.3s ease;
}
.card-beneficio:hover {
  transform: translateY(-8px);
}
.icon-circle {
  width: 70px;
  height: 70px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  color: white;
  background: linear-gradient(135deg, var(--color-primary), var(--color-secondary));
  font-size: 1.8rem;
  margin: 0 auto 20px;
}

/* ---- BOTONES ---- */
.btn-gradient {
  background: linear-gradient(90deg, var(--color-primary), var(--color-secondary));
  color: white;
  border: none;
  border-radius: 25px;
  padding: 10px 24px;
  transition: all 0.3s ease;
}
.btn-gradient:hover {
  transform: scale(1.05);
  box-shadow: 0 0 15px rgba(0,191,255,0.4);
}

/* ---- SIMULADOR ---- */
.simulador {
  background: linear-gradient(180deg, var(--color-dark), #000e1f);
}
.simulador input {
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.2);
  color: white;
}
.simulador input::placeholder {
  color: rgba(255,255,255,0.5);
}

/* ---- FOOTER ---- */
.footer-tech {
  background: #00182a;
  border-top: 1px solid rgba(255, 255, 255, 0.1);
  color: var(--text-muted);
  text-align: center;
  padding: 40px 0;
}
.footer-tech a {
  color: var(--color-accent);
  transition: color 0.3s;
}
.footer-tech a:hover {
  color: var(--color-secondary);
}

/* ---- ANIMACIONES ---- */
.animate-fadeup {
  animation: fadeUp 1s ease-in-out;
}
@keyframes fadeUp {
  0% { opacity: 0; transform: translateY(20px); }
  100% { opacity: 1; transform: translateY(0); }
}
